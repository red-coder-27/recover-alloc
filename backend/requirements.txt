# RECOVER-ALLOC backend dependencies — Locked spec Part B.
# NOTE: this repo was authored in a network-isolated sandbox that could not
# actually `pip install` any of these to verify them (see docs/PHASE1_REPORT.md).
# Version pins below are chosen for mutual compatibility as of the spec date;
# re-verify with `pip install -r requirements.txt` in a real environment
# before relying on exact versions.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
ortools==9.15.6755
scikit-learn==1.5.2
numpy==1.26.4
anthropic==0.34.2
python-dotenv==1.0.1
requests==2.32.3
pytest==8.3.3
pytest-cov==5.0.0
